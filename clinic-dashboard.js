(()=>{
const API='/api/clinic';
const ADMIN_EMAIL='shabankrasniqifizoterapi@gmail.com';
const esc=s=>String(s??'').replace(/[&<>\"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;'}[c]));
const date=d=>d?new Intl.DateTimeFormat('sq-AL',{weekday:'long',day:'numeric',month:'long',hour:'2-digit',minute:'2-digit'}).format(new Date(d)):'—';
const day=d=>d?new Intl.DateTimeFormat('sq-AL',{day:'2-digit',month:'short'}).format(new Date(d)):'—';
async function token(){if(!window.Clerk?.session)return null;return Clerk.session.getToken();}
async function api(options={}){const t=await token();if(!t)throw Error('Sesioni Clerk mungon.');const r=await fetch(API,{...options,headers:{Authorization:`Bearer ${t}`,'Content-Type':'application/json',...(options.headers||{})}});const data=await r.json().catch(()=>({}));if(!r.ok)throw Error(data.error||'Kërkesa dështoi.');return data;}
function inject(){if(document.getElementById('clinic-live-style'))return;const s=document.createElement('style');s.id='clinic-live-style';s.textContent=`.live-wrap{margin-top:16px;display:grid;grid-template-columns:1.25fr .75fr;gap:16px}.live-card{border:1px solid var(--line);background:linear-gradient(180deg,rgba(17,25,37,.88),rgba(12,18,27,.88));border-radius:22px;padding:23px}.live-card h2{font:700 1rem Montserrat,sans-serif;margin:0 0 7px}.live-head{display:flex;justify-content:space-between;gap:15px;margin-bottom:18px}.live-list{display:grid;gap:10px}.live-item{padding:14px;border:1px solid var(--line);border-radius:15px;background:rgba(7,11,18,.35)}.live-item strong{display:block;margin-bottom:4px}.live-item small,.live-item p{color:var(--muted);margin:0;font-size:.78rem;line-height:1.55}.live-pill{display:inline-flex;padding:5px 8px;border-radius:999px;background:rgba(114,217,154,.1);color:var(--green);font-size:.66rem;font-weight:800}.live-form{display:grid;gap:9px}.live-form input,.live-form textarea{width:100%;box-sizing:border-box;border:1px solid var(--line);background:#090f18;color:var(--text);border-radius:11px;padding:11px 12px;font:inherit}.live-form textarea{min-height:88px;resize:vertical}.live-btn{border:0;border-radius:11px;padding:12px 14px;background:#f5f7fa;color:#080b11;font:800 .86rem Manrope;cursor:pointer}.live-msg{font-size:.78rem;color:var(--muted);min-height:18px}.exercise{margin-top:9px;padding:11px;border-radius:12px;background:#0b111a;border:1px solid var(--line)}.exercise b{font-size:.83rem}.exercise span{display:block;color:var(--muted);font-size:.74rem;margin-top:4px}.live-muted{color:var(--muted);font-size:.82rem}@media(max-width:850px){.live-wrap{grid-template-columns:1fr}}`;document.head.append(s)}
async function load(){inject();const data=await api();const content=document.getElementById('content');if(!content)return;const existing=document.getElementById('clinic-live');existing?.remove();const appointments=(data.appointments||[]).filter(x=>!['cancelled','no_show'].includes(x.status));const plans=data.treatmentPlans||[];const messages=data.messages||[];const docs=data.documents||[];const next=appointments[0];const wrap=document.createElement('section');wrap.id='clinic-live';wrap.className='live-wrap';wrap.innerHTML=`<article class="live-card"><div class="live-head"><div><h2>Terminet e mia</h2><div class="live-muted">Të dhëna reale nga profili yt.</div></div><span class="live-pill">Supabase · Live</span></div><div class="live-list">${appointments.length?appointments.map(a=>`<div class="live-item"><strong>${esc(a.service)} · ${esc(a.location)}</strong><small>${esc(date(a.starts_at))} · ${esc(a.duration_minutes)} min</small><p>${esc(a.status==='requested'?'Kërkesë në pritje':a.status==='confirmed'?'Konfirmuar':a.status==='completed'?'Përfunduar':'Planifikuar')}</p></div>`).join(''):'<div class="live-item"><strong>Nuk ka termine</strong><small>Kërko një termin të ri më poshtë.</small></div>'}</div></article><article class="live-card"><div class="live-head"><div><h2>Kërko termin</h2><div class="live-muted">Dërgo kërkesë direkt te fizioterapeuti.</div></div></div><form class="live-form" id="appointment-form"><input id="appt-date" type="datetime-local" required><input id="appt-service" placeholder="Shërbimi" value="Fizioterapi"><input id="appt-location" placeholder="Lokacioni" value="Klinika"><textarea id="appt-notes" placeholder="Shënim opsional"></textarea><button class="live-btn">Dërgo kërkesën</button><div class="live-msg" id="appt-msg"></div></form></article><article class="live-card"><div class="live-head"><div><h2>Plani im i trajtimit</h2><div class="live-muted">Ushtrimet dhe objektivat e caktuara për ty.</div></div></div><div class="live-list">${plans.length?plans.map(p=>`<div class="live-item"><strong>${esc(p.title)} <span class="live-pill">${esc(p.status)}</span></strong><p>${esc(p.summary||p.goals||'Pa përshkrim.')}</p>${(p.treatment_exercises||[]).map(e=>`<div class="exercise"><b>${esc(e.name)}</b><span>${esc(e.instructions||'')} ${e.sets?`· ${e.sets} seri`:''} ${e.repetitions?`· ${e.repetitions} përsëritje`:''} ${esc(e.frequency||'')}</span></div>`).join('')}</div>`).join(''):'<div class="live-item"><strong>Plani ende nuk është caktuar.</strong><small>Do të shfaqet këtu sapo fizioterapeuti ta shtojë.</small></div>'}</div></article><article class="live-card"><div class="live-head"><div><h2>Dokumentet</h2><div class="live-muted">Dokumente private të lidhura me profilin.</div></div></div><div class="live-list">${docs.length?docs.map(d=>`<div class="live-item"><strong>${esc(d.title)}</strong><small>${esc(d.mime_type||'Dokument')} · ${esc(day(d.uploaded_at))}</small></div>`).join(''):'<div class="live-item"><strong>Nuk ka dokumente.</strong><small>Dokumentet që të caktohen do të shfaqen këtu.</small></div>'}</div></article><article class="live-card"><div class="live-head"><div><h2>Mesazhet</h2><div class="live-muted">Komunikim direkt me klinikën.</div></div></div><div class="live-list">${messages.slice(0,8).map(m=>`<div class="live-item"><strong>${m.sender_role==='admin'?'Fizioterapeuti':'Ti'}</strong><p>${esc(m.message)}</p><small>${esc(date(m.created_at))}</small></div>`).join('')||'<div class="live-item"><strong>Asnjë mesazh ende.</strong></div>'}<form class="live-form" id="message-form" style="margin-top:10px"><textarea id="message" placeholder="Shkruaj një mesazh…" required></textarea><button class="live-btn">Dërgo mesazhin</button><div class="live-msg" id="message-msg"></div></form></div></article><article class="live-card"><div class="live-head"><div><h2>Profili klinik</h2><div class="live-muted">Të dhënat bazë që përdor klinika.</div></div></div><form class="live-form" id="profile-form"><input id="phone" placeholder="Telefon" value="${esc(data.patient?.phone||'')}"><input id="dob" type="date" value="${esc(data.patient?.date_of_birth||'')}"><textarea id="notes" placeholder="Shënime për klinikën">${esc(data.patient?.notes||'')}</textarea><button class="live-btn">Ruaj profilin</button><div class="live-msg" id="profile-msg"></div></form></article>`;const security=content.querySelector('.security');security?content.insertBefore(wrap,security):content.append(wrap);
const form=document.getElementById('appointment-form');form?.addEventListener('submit',async e=>{e.preventDefault();const msg=document.getElementById('appt-msg');msg.textContent='Po dërgohet…';try{await api({method:'POST',body:JSON.stringify({action:'appointment-request',starts_at:document.getElementById('appt-date').value,duration_minutes:60,service:document.getElementById('appt-service').value,location:document.getElementById('appt-location').value,notes:document.getElementById('appt-notes').value})});msg.textContent='✓ Kërkesa u dërgua. Do të konfirmohet nga klinika.';setTimeout(load,500)}catch(err){msg.textContent=err.message}});
const mf=document.getElementById('message-form');mf?.addEventListener('submit',async e=>{e.preventDefault();const msg=document.getElementById('message-msg');msg.textContent='Po dërgohet…';try{await api({method:'POST',body:JSON.stringify({action:'message',message:document.getElementById('message').value})});msg.textContent='✓ Mesazhi u dërgua.';document.getElementById('message').value='';setTimeout(load,500)}catch(err){msg.textContent=err.message}});
const pf=document.getElementById('profile-form');pf?.addEventListener('submit',async e=>{e.preventDefault();const msg=document.getElementById('profile-msg');msg.textContent='Po ruhet…';try{await api({method:'PATCH',body:JSON.stringify({action:'profile',phone:document.getElementById('phone').value,date_of_birth:document.getElementById('dob').value,notes:document.getElementById('notes').value})});msg.textContent='✓ Profili u ruajt.'}catch(err){msg.textContent=err.message}});
}
function start(){if(!window.Clerk)return setTimeout(start,150);if(Clerk.isSignedIn)load().catch(e=>console.warn('[clinic dashboard]',e));else setTimeout(start,500)}
start();
})();
